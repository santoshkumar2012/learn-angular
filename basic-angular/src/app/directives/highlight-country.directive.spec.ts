import { HighlightCountryDirective } from './highlight-country.directive';

describe('HighlightCountryDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCountryDirective();
    expect(directive).toBeTruthy();
  });
});
